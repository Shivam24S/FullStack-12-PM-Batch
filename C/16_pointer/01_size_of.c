

#include <stdio.h>

int main()
{

    int a = 24;

    char word = 's';

    double number = 10;

    float pi = 3.14;

    printf("\n the size of int datatypes will be %d", sizeof(a));

    printf("\n the size of char datatypes will be %d", sizeof(word));

    printf("\n the size of double datatypes will be %d", sizeof(number));

    printf("\n the size of float datatypes will be %d", sizeof(pi));

    return 0;
}