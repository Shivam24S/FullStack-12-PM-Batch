var activity = "hi there";

function currentActivity() {
  if (true) {
    var running = "running";
  }

  console.log("running", running);
}

// console.log("activity :-", activity);

currentActivity();
